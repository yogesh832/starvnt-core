import { GoogleGenAI } from '@google/genai'; import dotenv from 'dotenv'; dotenv.config(); const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY }); async function run() { try { const response = await ai.models.list(); for (let m of response.models || response.data || Object.values(response)) { if (m && m.name) console.log(m.name); else if (Array.isArray(m)) m.forEach(x => console.log(x.name)); } } catch (err) { console.error(err); } } run();

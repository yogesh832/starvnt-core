import { OpenAI } from 'openai'; import dotenv from 'dotenv'; dotenv.config(); const openai = new OpenAI({ apiKey: process.env.GEMINI_API_KEY, baseURL: 'https://generativelanguage.googleapis.com/v1beta/openai/' }); async function run() { try { const res = await openai.chat.completions.create({ model: 'gemini-3.5-flash', messages: [{role: 'user', content: 'test'}] }); console.log(res.choices[0].message.content); } catch (err) { console.error(err); } } run();
